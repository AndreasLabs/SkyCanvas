# Commented out for later use - custom build configuration
# services:
#   ardupilot-sil:
#     build:
#       context: .
#       dockerfile: ../ardupilot_sil/Dockerfile
#     volumes:
#       - ../ardupilot_sil/:/ardupilot
#     container_name: ardupilot-sil
#     network_mode: host
#     restart: 'always'
#     environment:
#     - COUNT=${NUMCOPTERS:-3}

# Using pre-built image from DockerHub
services:
  ardupilot-sitl:
    image: radarku/ardupilot-sitl
    platform: linux/amd64
    tty: true
    ports:
      - 5760:5760
    container_name: ardupilot-sitl
    restart: 'always'
    environment:
      - INSTANCE=0
     # - LAT=42.3898
    #  - LON=-71.1476
    #  - ALT=14
   #   - DIR=270
  #    - MODEL=+
      - SPEEDUP=2
      - VEHICLE=ArduCopter
