# Commented out for later use - custom build configuration
# services:
#   ardupilot-sil:
#     build:
#       context: .
#       dockerfile: ../ardupilot_sil/Dockerfile
#     volumes:
#       - ../ardupilot_sil/:/ardupilot
#     container_name: ardupilot-sil
#     network_mode: host
#     restart: 'always'
#     environment:
#     - COUNT=${NUMCOPTERS:-3}

# Using pre-built image from DockerHub
services:
  ardupilot-sitl:
    image: radarku/ardupilot-sitl
    tty: true
    ports:
      - 5760:5760
    container_name: ardupilot-sitl
    restart: 'always'
    environment:
      - INSTANCE=0
      - LAT=34.556685079351766
      - LON=-115.7423058300774
    #  - ALT=14
   #   - DIR=270
  #    - MODEL=+
      - SPEEDUP=4
      - VEHICLE=ArduCopter
